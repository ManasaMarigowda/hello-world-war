pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {
              sh "rm -rf * "
                sh "git clone https://github.com/ManasaMarigowda/hello-world-war"
            }
  }
        stage('build') {
            steps {
                sh "mvn clean package"
            }
       
        }
    }
}
